Package Overview
=================

A complete map of the ``solax`` package, mirroring its actual folder
structure -- including internals not importable at the top-level
``solax`` namespace (see :doc:`../solax` for those). Some classes/
functions here are also shown on the Public API page; where that
happens, this is noted explicitly.

.. toctree::
   :maxdepth: 3

   solax/index
